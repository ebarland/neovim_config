return {
	"nvim-treesitter/nvim-treesitter-context",
	branch = "master", -- optional, but keeps things explicit
	dependencies = { "nvim-treesitter/nvim-treesitter" },
	opts = {},       -- expands to require("treesitter-context").setup({})
}
